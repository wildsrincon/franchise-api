spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/franchise_test_db
      auto-index-creation: true

  main:
    banner-mode: off

logging:
  level:
    com.franchise: INFO
    org.springframework.data.mongodb: WARN
    org.springframework: WARN